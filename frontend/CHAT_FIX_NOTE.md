Chat latency diagnosis: production logs show POST /api/chat reaching OpenAI and returning HTTP 401 Unauthorized. This indicates the configured OpenAI credential is invalid/missing. The backend also reports Emergent storage 401. A valid provider credential is still required for arbitrary AI questions. Instant replies were added for common visitor questions so greetings/services/contact/supplies no longer wait on the failing provider.